# Path to your Excel workbook (relative to project root)
EXCEL_PATH=Career Tracker.xlsx

# Ollama model name (must be pulled via: ollama pull mistral)
OLLAMA_MODEL=mistral

# Max emails to fetch per scan (increase if needed)
MAX_EMAILS=20
